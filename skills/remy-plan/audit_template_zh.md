# 计划深度分析表格模板

你必须按以下顺序输出 5 张 Markdown 表格。**每张表格前后各空 1 行。**

### 🧩 表 1: 歧义消除矩阵

*   **目标**: 消除所有"TBD"（待定）。将选项转为硬约束。
*   **强制规则**: 若技术细节（超时、重试、具体库）未锁定，计划**被拒绝**。

| 决策点 | 选项/可能性 | 最终约束 | 理由 |
| :--- | :--- | :--- | :--- |
| *示例: 超时* | *默认 / 30秒 / 60秒* | ***锁定: 连接 15 秒，读取 30 秒*** | *避免资源耗尽* |
| *示例: 库选择* | *Json / Orjson* | ***锁定: 标准 json*** | *避免引入新依赖* |

### 🧪 表 2: PBT 属性规约

*   **目标**: 定义对所有输入恒为真的数学不变量（非仅限示例）。
*   **类别**: 幂等性、可逆性、守恒性、交换律。

| 功能模块 | PBT 属性类型 | 不变量描述 | 证伪策略 |
| :--- | :--- | :--- | :--- |
| *示例: 解析器* | *可逆性* | `decode(encode(x)) == x` | *随机 Unicode 字符串* |
| *示例: 钱包* | *守恒性* | `balance >= 0` 恒成立 | *并发扣减* |

### ⚖️ 表 3: 逻辑与契约审计

*   **数据流**: 验证上下游参数兼容性。
*   **系统风险**: 检查全局状态修改或操作系统特定假设。

| 维度 | 检查项 | 状态 | 决策/规约 |
| :--- | :--- | :--- | :--- |
| **数据流** | 上游依赖 / 下游兼容 | Pass/Warn | （必须定义具体数据契约） |
| **数据流** | 新增可变状态实体（schema 列 / 配置项 / sentinel 文件）是否同时列出 init / read / write 三路径 | Pass/Warn | Write 路径缺失时必须在表 1 显式声明该实体为 init-once read-only |
| **一致性** | 函数签名 / 库调用 | Pass/Fail | （递归对照定义检查） |
| **数据结构** | 硬编码 / 参数化 | Pass/Locked | （必须优先使用参数/配置而非硬编码） |
| **系统风险** | 副作用 / 环境兼容 | Pass/Warn | （检查全局机制与操作系统差异） |
| **复杂度** | 时间 / 空间 / OOM | Pass/Warn | （评估循环与内存使用） |
| **并发与锁** | 读写冲突 / 死锁 | Pass/Warn | （检查文件 IO 与共享资源） |
| **零决策** | 参数锁定 / 歧义消除 | Locked | （必须与表 1 匹配） |

### 🛠️ 表 4: 物理变更预演

*   **修改层级**: 以一个标签开头 — `[补丁]` / `[局部修复]` / `[接口变更]` / `[跨域重构]` — 后接 1 句话说明为何此修改位于该层级。参见 SKILL.md Step 2 的 Scope Tag Reference 表。
*   **涟漪效应**: 确认导入与依赖不产生循环引用。

| 文件路径 | 定位 | 操作 | 简述 | 修改层级 | 涟漪效应 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `path/to/file` | `func_name` | Modify | 增加重试逻辑 | [局部修复] 在产生错误状态的函数内增加重试 | 无 |

**Caller Refs 注解**（`操作=Create` 行必填，参见 SKILL.md Step 2.9.2）

| Create 项 ID | caller_file（已存在于工作树） | caller_function | evidence_ref |
| :--- | :--- | :--- | :--- |
| *示例: C-002* | *path/to/existing_file.py* | *EntryClass.run* | *E-003* |

如本 packet 不含任何 `Create` 行，本注解表为空。**禁止 orphan creation**：`caller_file` 必须是当前工作树已存在的文件，不能是同 packet 中其他 `Create` 项创建的新文件。

### ✅ 表 5: 验证计划

*   **目标**: 定义实现完成后的端到端验证方法。
*   **范围**: 每个条目对应表 4 中一项或多项变更。

| 验证步骤 | 方法 | 预期结果 | 回退条件 |
| :--- | :--- | :--- | :--- |
| *示例: 单元测试* | `pytest tests/test_auth.py -v` | 全部测试通过 | 回退该 commit |
| *示例: 集成测试* | 手工调用修改后的端点 | 响应符合表 2 不变量 | 标记为部分实现 |
| *示例: 回归测试* | 运行全部测试集 | 无新增失败 | 排查后再决定是否继续 |

---

> **⚠ 检查点**: 5 张表已全部完成。**不要**输出停止提示。**必须**进入 **Section 5 (Evidence Packet Generation)** 并执行 5 步（时间戳 → git commit → 创建目录 → 写入 packet → 更新 .active_packet），最后才输出停止提示。
