# Security Audit Report
**Generated**: {{ timestamp }}
**Effort Level**: {{ effort_level }}
**Diff Range**: `{{ diff_range }}`
**Files Analyzed**: {{ file_count }}

---

## Summary

| Metric | Value |
| :--- | :--- |
| Pre-Scan Findings | {{ prescan_count }} |
| Agent Findings (raw) | {{ agent_raw_count }} |
| Agent Findings (verified) | {{ agent_verified_count }} |
| Confidence Threshold | {{ threshold }}/10 |
| **Total Reported** | **{{ total_findings }}** (HIGH: {{ high_count }}, MEDIUM: {{ medium_count }}) |

---

## Findings

{% for finding in findings %}
### Vuln {{ loop.index }}: {{ finding.category }}: `{{ finding.file }}:{{ finding.line }}`

* **Severity**: {{ finding.severity }}
* **Confidence**: {{ finding.confidence }}/10
* **Source**: {{ finding.source }}
* **Description**: {{ finding.description }}
* **Exploit Scenario**: {{ finding.exploit_scenario }}
* **Recommendation**: {{ finding.recommendation }}

{% endfor %}
{% if not findings %}
No vulnerabilities found above the confidence threshold.
{% endif %}

---

## Pre-Scan Details

{% if prescan_findings %}
| ID | Pattern | Severity | File | Line |
| :--- | :--- | :--- | :--- | :--- |
{% for p in prescan_findings %}
| {{ p.id }} | {{ p.name }} | {{ p.severity }} | `{{ p.file }}` | {{ p.line }} |
{% endfor %}
{% else %}
No deterministic pattern matches found.
{% endif %}

---

*Report generated by `/security-audit` skill.*
