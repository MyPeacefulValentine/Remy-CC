{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Precedent judgments for security-audit contextual filtering. These provide guidance on common patterns that are often flagged but rarely exploitable.",
  "precedents": [
    {
      "id": "PR-001",
      "rule": "Logging high-value secrets (API keys, passwords, tokens) in plaintext is a vulnerability. Logging URLs or non-sensitive metadata is safe.",
      "applies_to": "data_exposure"
    },
    {
      "id": "PR-002",
      "rule": "UUIDs can be assumed unguessable. They do not need additional validation or obfuscation.",
      "applies_to": "auth"
    },
    {
      "id": "PR-003",
      "rule": "Environment variables and CLI flags are trusted values. Attacks requiring control of an environment variable are invalid.",
      "applies_to": "injection"
    },
    {
      "id": "PR-004",
      "rule": "Resource management issues (memory leaks, file descriptor leaks) are not security vulnerabilities.",
      "applies_to": "general"
    },
    {
      "id": "PR-005",
      "rule": "Subtle or low-impact web vulnerabilities (tabnabbing, XS-Leaks, prototype pollution, open redirects) should not be reported unless extremely high confidence.",
      "applies_to": "injection"
    },
    {
      "id": "PR-006",
      "rule": "React and Angular are secure against XSS by default. Do not report XSS unless dangerouslySetInnerHTML, bypassSecurityTrustHtml, or similar unsafe methods are used.",
      "applies_to": "injection"
    },
    {
      "id": "PR-007",
      "rule": "Most GitHub Action workflow vulnerabilities are not exploitable in practice. Require a concrete and specific attack path.",
      "applies_to": "injection"
    },
    {
      "id": "PR-008",
      "rule": "Lack of permission checking in client-side JS/TS is not a vulnerability. Server-side is responsible for authorization.",
      "applies_to": "auth"
    },
    {
      "id": "PR-009",
      "rule": "Only report MEDIUM findings if they are obvious and concrete issues with clear exploitation paths.",
      "applies_to": "general"
    },
    {
      "id": "PR-010",
      "rule": "Vulnerabilities in Jupyter notebooks (*.ipynb) are rarely exploitable. Require a concrete attack path where untrusted input triggers the vulnerability.",
      "applies_to": "general"
    },
    {
      "id": "PR-011",
      "rule": "Logging non-PII data is not a vulnerability even if the data may be sensitive. Only report logging of secrets, passwords, or PII.",
      "applies_to": "data_exposure"
    },
    {
      "id": "PR-012",
      "rule": "Command injection in shell scripts is generally not exploitable since shell scripts rarely run with untrusted user input. Require a concrete attack path.",
      "applies_to": "injection"
    }
  ]
}
