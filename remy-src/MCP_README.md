# remy-index MCP Server

A stdio-based [Model Context Protocol](https://modelcontextprotocol.io/) server that exposes code intelligence queries from the remy-index SQLite database. Claude Code communicates with this server over JSON-RPC to query symbol definitions, call graphs, and impact analysis without subprocess overhead.

## Prerequisites

| Requirement | Notes |
| :--- | :--- |
| Python 3.10+ | Required by the `mcp` SDK |
| `mcp` package | `pip install mcp` (FastMCP stdio transport) |
| `logic_index.db` | Generated by `/remy-index` or `struct_scan.py` |

The server exits gracefully (exit code 0) if `mcp` is not installed or if `MCP_SERVER_ENABLED=false`.

## Architecture

```
┌─────────────────────┐       JSON-RPC (stdio)       ┌──────────────────────────┐
│   Claude Code       │ ◄──────────────────────────► │  index_mcp_server.py     │
│   (MCP client)      │                              │  ├─ _init_freshness()    │
└─────────────────────┘                              │  ├─ 8 tool handlers      │
                                                     │  └─ _with_freshness()    │
                                                     └──────────┬───────────────┘
                                                                │ import
                                                     ┌──────────▼───────────────┐
                                                     │  index_mcp_queries.py    │
                                                     │  ├─ SQL queries          │
                                                     │  ├─ BFS traversal        │
                                                     │  └─ result formatting    │
                                                     └──────────┬───────────────┘
                                                                │ import
                                                     ┌──────────▼───────────────┐
                                                     │  impact.py + struct_scan │
                                                     │  (shared BFS / helpers)  │
                                                     └──────────┬───────────────┘
                                                                │ read-only
                                                     ┌──────────▼───────────────┐
                                                     │  .claude/logic_index.db  │
                                                     │  (SQLite, WAL mode)      │
                                                     └──────────────────────────┘
```

**Data flow**: `struct_scan.py` (triggered at SessionStart or by dirty-file consumer) writes symbols, edges, and patterns into `logic_index.db`. The MCP server opens the database in read-only WAL mode and serves queries. The two processes never write concurrently to the same file.

## Startup & Registration

The server is registered in `~/.claude.json` during installation:

```json
{
  "mcpServers": {
    "remy-index": {
      "type": "stdio",
      "command": "python",
      "args": ["-u", "~/.claude/remy-src/index_mcp_server.py"]
    }
  }
}
```

- Claude Code launches the server automatically at session start.
- The `-u` flag forces unbuffered stdout, ensuring JSON-RPC responses are flushed immediately to the pipe.
- The server runs for the lifetime of the Claude Code session.

### Startup Sequence

1. Import check: if `mcp` package is missing, print error to stderr and `sys.exit(0)`.
2. Environment check: if `MCP_SERVER_ENABLED=false`, exit silently.
3. **`_init_freshness()`**: Probes index staleness via subprocess (git commit comparison or hash sampling). This MUST happen before the event loop starts (see [Troubleshooting](#troubleshooting)).
4. **`mcp.run(transport="stdio")`**: Enters the asyncio event loop and begins accepting JSON-RPC requests.

## Tool Reference

### query_symbol

Find symbol definitions by name.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `name` | `str` | (required) | Symbol name, short name, or qualified name (`file::name`) |
| `file` | `str` | `""` | Filter results to a specific file path |

**Output example:**
```
symbols matching 'parse_file' (2 results)

  [function] src/parser.py::parse_file(path, encoding='utf-8')  src/parser.py:L42-L87 (Core)
        Parse a source file and return an AST node.
  [method] src/loader.py::Loader.parse_file(self, path)  src/loader.py:L120-L145 (IO)
        Delegate to parser with loader-specific options.
```

---

### query_symbol_summary

Get symbol-level summary and docstring without reading the source file.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `name` | `str` | (required) | Symbol name or qualified name |
| `file` | `str` | `""` | Filter to a specific file |

**Output example:**
```
summary for 'bfs_callers'

  [function] skills/remy-index/impact.py::bfs_callers(db, target_set, max_depth, static_only=False)  L55
  summary: BFS traversal upstream from target symbols. Returns dict[depth] -> list[qualified_name].
```

---

### query_file_summary

Get file-level semantic summary (role, key symbols, layer) without reading the full source.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `file` | `str` | (required) | Repo-relative file path (`\` is auto-normalized to `/`) |

**Output example:**
```
## skills/remy-index/impact.py (8 symbols, layer=Core)
  short: BFS-based impact analysis over the call graph.
  full: Provides bfs_callers / bfs_callees / format_output utilities used by /remy-plan and /remy-patch.
```

Errors: returns `No file '<path>' in index. Run /remy-index to scan.` when the path is not indexed.

---

### query_callers

Find upstream callers of a symbol via BFS, grouped by depth level.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `symbol` | `str` | (required) | Symbol name or qualified name |
| `depth` | `int` | `2` | Max BFS depth (clamped to `MCP_BFS_MAX_DEPTH`) |
| `include_ambiguous` | `bool` | `False` | Include edges resolved via `edge_candidates` table |
| `static_only` | `bool` | `False` | Exclude synthesized edges (provenance: inferred/speculative) |

**Output example:**
```
callers of parse_file (2 levels, 5 results)

[depth 1] direct:
  src/loader.py::Loader.load [L30-L55] (IO)
  src/cli.py::main [L10-L25] (Entry)

[depth 2]
  tests/test_loader.py::test_load_valid [L15-L30] (Test)
  tests/test_cli.py::test_main_happy [L8-L20] (Test)
  src/app.py::Application.start [L100-L130] (Entry)
```

---

### query_callees

Find downstream callees of a symbol via BFS.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `symbol` | `str` | (required) | Symbol name or qualified name |
| `depth` | `int` | `2` | Max BFS depth (clamped to `MCP_BFS_MAX_DEPTH`) |
| `include_ambiguous` | `bool` | `False` | Include ambiguous resolution candidates |
| `static_only` | `bool` | `False` | Exclude synthesized edges |

Output format is identical to `query_callers`.

---

### query_impact

Analyze the impact radius of modifying one or more files. Shows both upstream callers and downstream callees of all symbols defined in the target files.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `files` | `list[str]` | (required) | File paths (forward slashes, relative to project root) |
| `depth_up` | `int` | `3` | Upstream BFS depth |
| `depth_down` | `int` | `3` | Downstream BFS depth |
| `include_ambiguous` | `bool` | `False` | Include ambiguous edges |
| `static_only` | `bool` | `False` | Exclude synthesized edges |

**Output example:**
```
impact analysis for: src/parser.py

upstream (callers into these files):
  [depth 1] src/loader.py, src/cli.py ... +2
  [depth 2] src/app.py, tests/test_loader.py ... +3

downstream (called by these files):
  [depth 1] src/ast_nodes.py, src/utils.py
  [depth 2] src/tokenizer.py

summary: 8 files affected, 7 upstream + 3 downstream symbols
```

---

### query_patterns

Query event/callback registration patterns (Django signals, PyQt signals, observer pattern).

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `pattern_type` | `str` | `""` | Filter by type: `django_signal`, `pyqt_signal`, `observer` |
| `signal_name` | `str` | `""` | Filter by signal name |
| `file` | `str` | `""` | Filter by file path |

**Output example:**
```
event/callback patterns (3 results)

  [django_signal] post_save -> handle_user_created  src/signals.py:L15
  [pyqt_signal] clicked -> on_button_click  src/ui/main_window.py:L42
  [observer] on_data_changed -> DataView.refresh  src/views.py:L88
```

---

### query_search

Fuzzy search symbols by name. Uses a three-tier fallback strategy:
1. **FTS5 prefix match** (fastest, leverages full-text index)
2. **LIKE substring match** (if FTS returns nothing)
3. **Edit-distance match** (if LIKE returns nothing, catches typos)

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `text` | `str` | (required) | Search query (partial name, prefix, or approximate) |
| `limit` | `int` | `10` | Maximum results to return |
| `file_hint` | `str` | `""` | Substring filter on file paths |

**Output example:**
```
search results for 'pars_fil' (2 results, matched via fuzzy)

  [function] src/parser.py::parse_file  src/parser.py:L42 (Core)
  [method] src/loader.py::Loader.parse_file  src/loader.py:L120 (IO)
```

---

### query_flow

Find call paths among two or more named symbols via bidirectional BFS.

| Parameter | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `symbols` | `list[str]` | (required, min 2) | Symbol names in traversal order |
| `max_depth` | `int` | `15` | Max BFS depth per pair |
| `max_visited` | `int` | `2000` | Max nodes visited per pair (prevents combinatorial explosion) |
| `static_only` | `bool` | `False` | Exclude synthesized edges |

**Symbol syntax:**
- Bare name: `parse_file`
- Qualified: `src/parser.py::parse_file`
- Class.method: `Loader.parse_file`
- File hint: `parser.py:parse_file`

**Output example:**
```
## Flow (call path among queried symbols)

1. main (src/cli.py:10)
   ↓ call
2. load (src/loader.py:30)
   ↓ call
3. parse_file (src/parser.py:42)
   ↓ call
4. tokenize (src/tokenizer.py:15)
```

When symbols are not connected:
```
## Flow (partial — 2/3 symbols connected)

1. main (src/cli.py:10)
   ↓ call
2. parse_file (src/parser.py:42)

[Break: pair (parse_file, unrelated_func) not connected within depth=15]

3. unrelated_func (src/other.py:5)
```

## Configuration

All parameters are set via environment variables in the `env` block of `settings.local.json` (or globally in `settings.json`). Editable via `remy-cc config` UI under the "MCP Server" group.

| Variable | Default | Description |
| :--- | :--- | :--- |
| `MCP_SERVER_ENABLED` | `true` | Set to `false` to disable the MCP server entirely |
| `MCP_BFS_MAX_DEPTH` | `5` | Hard cap on BFS depth for callers/callees/impact |
| `MCP_IMPACT_MAX_DEPTH_UP` | `3` | Default upstream depth for `query_impact` |
| `MCP_IMPACT_MAX_DEPTH_DOWN` | `3` | Default downstream depth for `query_impact` |
| `MCP_RESULT_LIMIT` | `50` | Max results per depth level in BFS output |
| `MCP_STATIC_ONLY_DEFAULT` | `false` | Default value for `static_only` when not specified |
| `FLOW_MAX_DEPTH` | `15` | Default max BFS depth for `query_flow` |
| `FLOW_MAX_VISITED` | `2000` | Default max visited nodes for `query_flow` |
| `LOGIC_INDEX_DB_PATH` | `.claude/logic_index.db` | Relative path to the SQLite database |

Related variable (in "Context Injection" group):

| Variable | Default | Description |
| :--- | :--- | :--- |
| `NAV_MCP_MINIMAL_ENABLED` | `true` | When MCP is available, inject only cluster overview (~1 KB) instead of full symbol tree (~40 KB) |

## Index Staleness Detection

On startup, `_init_freshness()` checks whether the index is current:

```
┌─────────────────────────────┐
│ Read source_commit from DB  │
└──────────┬──────────────────┘
           │
     ┌─────▼─────┐     yes    ┌──────────────────┐
     │ git avail?├────────────│ Compare HEAD vs  │
     └─────┬─────┘            │ source_commit    │
           │ no               └────────┬─────────┘
           │                           │
           │                     ┌─────▼──────┐
  ┌────────▼───────────┐         │  Match?    │
  │ Hash sampling      │         └──┬──────┬──┘
  │ (10% of files,     │         yes│      │no
  │  max 10 samples)   │            │      ▼
  └────────┬───────────┘            │  ┌──────────────────────┐
           │                        │  │ Warning: commit drift│
           │                        │  └──────────────────────┘
  ┌────────▼───────────┐            │
  │ >20% differ?       │   ┌────────▼─────────────┐
  │ >50% differ?       │   │ Check git status     │
  └────────────────────┘   │ (porcelain)          │
                           └────────┬─────────────┘
                                    │
                              ┌─────▼──────┐
                              │ >20% dirty?│──► Warning
                              └────────────┘
```

When a warning is generated, it is prepended to every tool response:

```
[Warning: index built at commit a1b2c3d4, current HEAD is e5f6g7h8. Run /remy-index to rebuild.]

symbols matching 'parse_file' (1 results)
  ...
```

The warning does NOT block tool usage — results are still returned, but may be incomplete or stale.

## MCP Minimal Mode

When the MCP server is running, the context injection system (`injector.py`) switches from injecting the full logic tree (~40 KB of symbol signatures and summaries) to a minimal payload (~1 KB):
- Cluster overview table (cluster name, file count, entry files)
- MCP tool usage hints (when to use which tool)

This reduces the baseline context consumption while maintaining full query capability on demand. Control with `NAV_MCP_MINIMAL_ENABLED` (project-level setting).

## Troubleshooting

### First tool call hangs indefinitely (Windows)

**Symptom**: The first MCP tool call never returns. Subsequent calls are never attempted.

**Root cause**: `subprocess.run(capture_output=True)` inside an asyncio tool handler deadlocks on Windows. The ProactorEventLoop uses I/O completion ports to manage the MCP server's stdin/stdout pipes. Creating additional pipes via `capture_output=True` in the same process conflicts with the event loop's pipe management, causing the subprocess read to never complete.

**Fix** (applied in v1.4.3): All subprocess calls were moved to `_init_freshness()`, which executes before `mcp.run()` starts the event loop.

**If you encounter similar symptoms after modifications**: Verify that no `subprocess.run`, `subprocess.Popen`, or `os.popen` calls exist in any tool handler code path.

### "Error: logic_index.db not found"

The database does not exist at the expected path. Run `/remy-index` to generate it, or verify `LOGIC_INDEX_DB_PATH` points to the correct location.

### Stale results after code changes

The index is built from a snapshot. If you modify source files after the last scan:
1. The staleness warning will appear (if >20% files differ).
2. Run `/remy-index` to rebuild, or wait for the next SessionStart auto-scan.

### MCP server not starting

Check:
1. `python --version` >= 3.10
2. `pip show mcp` confirms the package is installed
3. `~/.claude.json` contains the `remy-index` entry under `mcpServers`
4. `MCP_SERVER_ENABLED` is not set to `false`

Diagnostic: run manually and check stderr:
```bash
python -u ~/.claude/remy-src/index_mcp_server.py
```

## For Developers

### Source Files

| File | Lines | Role |
| :--- | :--- | :--- |
| `index_mcp_server.py` | ~200 | FastMCP server definition, tool handlers (thin wrappers), freshness initialization |
| `index_mcp_queries.py` | ~830 | All query implementations: SQL, BFS traversal, symbol resolution, result formatting |

### Architectural Constraints

**INV-1**: Tool handler code paths MUST NOT call `subprocess.run`, `subprocess.Popen`, or any API that creates OS pipes. This invariant prevents the Windows ProactorEventLoop deadlock. Violation will cause the server to hang on the first tool call on Windows.

**INV-2**: `_freshness_warning` is written once by `_init_freshness()` before the event loop starts, and never modified afterward. Tool handlers only read this value.

**INV-3**: `_with_freshness(result)` skips the warning prefix when `result.startswith("Error:")` — error messages are returned as-is.

**INV-4**: All tool handlers are synchronous `def` (not `async def`). FastMCP runs them in a thread pool. SQLite operations do not conflict with asyncio.

### Adding a New Tool

1. Implement `query_xxx_impl(...)` in `index_mcp_queries.py`.
2. Add the handler in `index_mcp_server.py`:
   ```python
   @mcp.tool()
   def query_xxx(param: str, ...) -> str:
       """One-line description for MCP tool listing."""
       return _with_freshness(query_xxx_impl(param, ...))
   ```
3. Register permission in `settings.example.json` (already covered by `mcp__remy-index__*` wildcard).
4. Update this README and the main project README.
5. Update `CLAUDE.md` instruction text (the `instructions` parameter in the `FastMCP()` constructor).

### Edge Confidence Tiers

The `edges` table has a `provenance` column with 4 confidence levels:

| Level | Meaning | `static_only=True` |
| :--- | :--- | :--- |
| `definite` | Direct AST-resolved call | Included |
| `probable` | Name-matched cross-file call | Included |
| `inferred` | Synthesized via interface dispatch, observer pattern, or signal connection | **Excluded** |
| `speculative` | Ambiguous resolution candidate (multiple targets) | **Excluded** |

When `static_only=True`, only `definite` and `probable` edges are traversed. This produces tighter, more reliable results at the cost of missing polymorphic dispatch paths.
