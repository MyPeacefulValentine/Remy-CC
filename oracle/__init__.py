"""Oracle contract for the R3 Python->Rust scanner migration.

Development-time package: not listed in install.py DEPLOY_DIRS and never
deployed. Owns the frozen-oracle manifest, the table-level normalized
comparator, and the performance baseline tooling consumed by the
R3.2-R3.4 differential acceptance tests.
"""
